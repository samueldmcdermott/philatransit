services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"   # localhost only — host nginx proxies here
    volumes:
      - ./data:/app/data         # persist trip stats across restarts
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=America/New_York
